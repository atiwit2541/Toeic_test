# ไม่บังคับ: ข้ามหน้า login ตอน npm run dev / preview ในเครื่อง (หน้าโปรดักชันบน Vercel ยังต้อง login)
VITE_SKIP_AUTH=1

# ตั้งบน Vercel เท่านั้น — รหัสผ่านสำหรับเข้าแอปหลัง deploy (ไม่ใส่ใน repo)
# TOEIC_LOGIN_PASSWORD=your-secret
